# AuthAnalyzer 使用指南

## 📦 安装与部署

### 前置要求

- **Java 8 或更高版本**
- **Maven 3.x**
- **Burp Suite Professional**（社区版可能功能受限）

### 构建步骤

#### 方式一：使用 Conda 环境（推荐，无需单独安装 Java/Maven）

1. **创建并激活 conda 环境**
   ```bash
   cd AuthAnalyzer-Dev
   conda env create -f environment.yml
   conda activate authanalyzer
   ```

2. **构建项目**
   ```bash
   mvn clean package
   ```

#### 方式二：已安装 Java 和 Maven

1. **克隆或下载项目**
   ```bash
   cd AuthAnalyzer-Dev
   ```

2. **使用 Maven 构建项目**
   ```bash
   mvn clean package
   ```

3. **查找生成的 Jar 文件**
   - 构建成功后，在 `target` 目录下会生成：
   - `AuthAnalyzer-1.1.14-jar-with-dependencies.jar`（用于加载到 Burp）

### 在 Burp Suite 中加载扩展

1. **打开 Burp Suite**
   - 启动 Burp Suite Professional

2. **进入 Extender 标签**
   - 点击顶部菜单栏的 `Extender` 标签

3. **添加扩展**
   - 点击 `Extensions` 子标签
   - 点击 `Add` 按钮
   - 在 `Extension type` 中选择 `Java`
   - 点击 `Select file...`，选择 `target/AuthAnalyzer-1.1.14-jar-with-dependencies.jar`
   - 点击 `Next`，等待加载完成

4. **验证安装**
   - 如果看到绿色的 `Loaded successfully` 提示，说明安装成功
   - 在 Burp Suite 底部标签栏会出现 `AuthAnalyzer` 标签
   - 点击该标签，应该能看到两个子标签：`Analyzer` 和 `UI Testing`

---

## 🔍 Analyzer 模块使用指南

### 模块概述

Analyzer 模块用于自动化检测认证绕过漏洞。它会：
- 拦截 Burp Suite 中的 HTTP 请求
- 为每个请求创建多个"会话(Session)"变体
- 修改每个 Session 的请求头、Token 等参数
- 比较修改后的响应与原始响应
- 判断是否存在认证绕过

### 基本使用流程

#### 步骤 1：创建 Session（会话）

1. **打开 Analyzer 标签**
   - 在 Burp Suite 底部点击 `AuthAnalyzer` → `Analyzer`

2. **创建新 Session**
   - 在配置面板顶部，点击 `+` 按钮（或右键点击 Session 标签页）
   - 输入 Session 名称（例如：`Session1`、`AdminSession`）
   - 点击 `OK`

3. **配置 Session 参数**
   - **Headers to Replace（替换请求头）**：
     - 输入要替换的请求头，每行一个
     - 例如：
       ```
       Authorization: Bearer token123
       Cookie: session=abc123
       ```
   
   - **Remove Headers（删除请求头）**：
     - 勾选 `Remove Headers`
     - 输入要删除的请求头名称，每行一个
     - 例如：
       ```
       Authorization
       X-Custom-Header
       ```
   
   - **Filter Requests with Same Header（过滤相同请求头）**：
     - 勾选后，如果请求包含相同的 Header，将跳过分析
   
   - **Restrict to Scope（限制范围）**：
     - 勾选后，只分析指定 URL 的请求
     - 输入 URL，例如：`https://example.com/api/`

#### 步骤 2：配置 Token/参数

1. **添加 Token**
   - 在 Session 配置面板中，点击 `Add Parameter` 按钮
   - 输入 Token 名称（例如：`csrf_token`、`session_id`）

2. **配置 Token 位置**
   - **Token Location（Token 位置）**：选择 Token 可能出现的位置
     - `Header`：HTTP 请求头
     - `Cookie`：Cookie 中
     - `Body`：请求体中
     - `URL`：URL 参数中
   
3. **配置 Token 值来源**
   - **Auto Extract（自动提取）**：
     - 从响应中自动提取 Token 值
     - 需要指定提取位置（Header/Body/Cookie）
     - 需要指定字段名（例如：`csrf_token`）
   
   - **Static Value（静态值）**：
     - 使用固定的 Token 值
     - 在文本框中输入值
   
   - **From To String（从字符串提取）**：
     - 使用正则表达式从响应中提取
     - 设置 `From String` 和 `To String`
   
   - **Prompt for Input（提示输入）**：
     - 每次需要时手动输入 Token 值

4. **其他 Token 选项**
   - **Remove Token（删除 Token）**：如果勾选，将删除该 Token
   - **Add if Not Exists（不存在时添加）**：如果请求中没有该 Token，自动添加
   - **URL Encoded（URL 编码）**：对 Token 值进行 URL 编码
   - **Case Sensitive（大小写敏感）**：Token 名称匹配时区分大小写

#### 步骤 3：配置请求过滤器

在配置面板**右侧**，有一个标题为 **"Filters"** 的区域（与 Sessions 标签页并排），可勾选/配置以下请求过滤器，只分析符合条件的请求：

> **提示**：若界面较窄，可能需要向右滚动配置面板才能看到 Filters 区域。

1. **Method Filter（方法过滤）**
   - 只分析指定的 HTTP 方法（GET、POST 等）

2. **Path Filter（路径过滤）**
   - 只分析匹配指定路径模式的请求

3. **Query Filter（查询参数过滤）**
   - 只分析包含特定查询参数的请求

4. **StatusCode Filter（状态码过滤）**
   - 只分析返回特定状态码的请求

5. **FileType Filter（文件类型过滤）**
   - 只分析特定文件类型的请求

6. **In Scope Filter（范围过滤）**
   - 只分析 Burp Suite Scope 中的请求

7. **Only Proxy Filter（仅代理过滤）**
   - 只分析通过 Proxy 的请求

#### 步骤 4：启动分析

1. **启动 Analyzer**
   - 点击配置面板顶部的 `▶ Start` 按钮
   - 按钮变为 `⏸ Pause`，状态显示为绿色的 `Analyzer Running`

2. **配置选项**
   - **Drop Original Requests（丢弃原始请求）**：
     - 勾选后，Proxy 中的原始请求将被丢弃
     - 只发送修改后的请求
     - **注意**：此选项会阻止正常浏览，谨慎使用

3. **开始测试**
   - 通过 Burp Suite Proxy 浏览目标网站
   - 或者在 Repeater/Scanner 中发送请求
   - Analyzer 会自动拦截并分析请求

#### 步骤 5：查看分析结果

1. **查看结果表格**
   - 在 `CenterPanel` 的左侧表格中，可以看到所有被分析的请求
   - 表格列包括：
     - `ID`：请求 ID
     - `Method`：HTTP 方法
     - `Host`：主机名
     - `Path`：请求路径
     - `Status`：原始请求状态码
     - `Length`：响应长度
     - 每个 Session 的状态列（`SAME`、`SIMILAR`、`DIFFERENT`、`NA`）

2. **理解状态含义**
   - **SAME（相同）**：响应体完全相同，可能存在认证绕过
   - **SIMILAR（相似）**：响应长度相似（±5%），可能存在部分绕过
   - **DIFFERENT（不同）**：响应明显不同，认证有效
   - **NA（不可用）**：无法比较（如原始请求无响应）

3. **查看详细请求/响应**
   - 点击表格中的一行
   - 右侧会显示：
     - **Original**：原始请求和响应
     - **各 Session**：修改后的请求和响应
   - 每个 Session 有 `Request` 和 `Response` 两个子标签

4. **对比视图**
   - 点击 `Compare View` 按钮，可以同时显示两个 Session
   - 勾选 `Show Diff`，可以高亮显示差异
   - 使用 `Sync Tabs` 可以同步切换 Request/Response 标签

5. **过滤和搜索**
   - **表格过滤**：
     - 点击过滤器图标，可以按状态、标记、重复等过滤
   - **搜索**：
     - 在搜索框输入关键词
     - 可以选择搜索范围：Path、Request、Response
     - 支持负向搜索（Negative Search）

6. **标记和注释**
   - 右键点击表格行，可以：
     - **Mark Row**：标记重要行
     - **Comment**：添加注释
     - **Repeat Request**：重新发送请求
     - **Delete Row**：删除行

7. **导出数据**
   - 点击 `Export Table Data` 按钮
   - 选择导出格式和内容
   - 保存到文件

#### 步骤 6：暂停和停止

1. **暂停分析**
   - 点击 `⏸ Pause` 按钮
   - Analyzer 会暂停处理新请求，但已处理的请求仍会显示

2. **停止分析**
   - 点击 `⏹ Stop` 按钮
   - Analyzer 完全停止，线程池关闭

---

## 🤖 UI Testing 模块使用指南

### 模块概述

UI Testing 模块提供基于 Selenium WebDriver 的浏览器自动化测试功能，可以：
- 自动启动浏览器（通过 Burp Proxy）
- 设置 Cookie 和身份信息
- 自动点击链接和按钮
- 与 Analyzer 数据联动，查看测试结果

**表格显示请求的前提**：① 流量必须经过 Burp Proxy；② Analyzer 必须已点击 Start 运行。

### 基本使用流程

#### 步骤 1：配置测试参数

1. **打开 UI Testing 标签**
   - 在 Burp Suite 底部点击 `AuthAnalyzer` → `UI Testing`

2. **配置基础参数**（左侧控制面板）
   - **Target URL**：目标 URL（要抓取/测试的页面）
     - 例如：`https://example.com/dashboard`
   
   - **Header(s) to Replace（Original）**：用户 A 的认证信息，格式与 Session 的 Headers to Replace 相同，每行一个 header 或分号分隔
     - 例如：`Cookie: access_token=xxx`、`Cookie: session=xxx`

#### 步骤 2：启动 Analyzer（必须）

1. **切换到 Analyzer 标签**
2. **点击 `▶ Start` 按钮**，使 Analyzer 处于运行状态
3. 若 Analyzer 未运行，即使流量经过 Burp，表格也不会显示任何请求

#### 步骤 3：执行抓取

1. **配置好 Target URL**（必填）
2. **配置 Header(s) to Replace**（可选，用于以指定身份抓取）
3. 点击 `抓取并点击页面链接` 按钮
4. 系统会：
   - **若代理未启动**：自动启动 Chrome（经 Burp Proxy 127.0.0.1:8080）
   - 导航至 Target URL
   - 若已配置 Headers to Replace，自动解析 Cookie 并注入浏览器
   - 查找页面上所有 `<a>` 标签，逐个点击，每次点击后返回目标页面
5. 所有请求经过 Burp Proxy，会出现在中间表格中，供 Analyzer 分析

#### 步骤 4：查看测试结果

1. **查看请求表格**
   - 中间表格显示所有**经过 Burp Proxy** 的请求
   - 前提：Analyzer 已启动

2. **选择 Session**
   - 在左侧控制面板的 Session 下拉框中选择 Session
   - 用于查看该 Session 的分析结果

3. **查看详情**
   - 点击表格中的一行
   - 右侧详情面板显示：
     - **Original**：原始请求和响应
     - **Session**：选中 Session 的分析结果
     - **Log**：执行日志

4. **与 Analyzer 联动**
   - Analyzer 运行且流量经过 Burp 时，请求会被自动分析
   - 可在 Analyzer 标签中查看详细分析结果

### 「抓取并点击页面链接」的作用

- **前提**：配置 Target URL
- **流程**：若代理未启动则自动启动 → 导航至 Target URL → 若已配置 Headers to Replace 则自动注入 Cookie → 查找所有 `<a>` 链接 → 逐个点击 → 每次点击后返回目标页面
- **效果**：以指定身份（Original 的认证信息）遍历页面所有链接，生成大量请求经过 Burp Proxy，被 Analyzer 捕获并分析
- **典型用途**：越权检测时，以不同身份（普通用户/管理员）分别抓取，生成请求供 Analyzer 对比分析

---

## 🔄 对称采集（平凡响应筛选）

### 模块概述

在越权检测中，部分 API 对任意身份都返回相同内容（如公开数据），这类「平凡响应」会干扰真实越权漏洞的发现。**对称采集**通过比较用户 A 与用户 B 各自的 Original 响应，筛掉平凡结果，并区分**垂直越权**与**水平越权**。

**适用场景**：合并布局（AuthAnalyzer → UI Testing），需两个浏览器分别以用户 A、B 身份访问相同 API。

### 基本概念

- **Run1**：浏览器 1，Original = 用户 A 的 Cookie，Session1 = 用户 B 的 Cookie
- **Run2**：浏览器 2，配置交换后 Original = 用户 B 的 Cookie，Session1 = 用户 A 的 Cookie
- **比较对象**：同一 endpoint 下，A 的 Original 响应 vs B 的 Original 响应

### 使用流程

#### 步骤 1：配置并启用对称采集

1. 在 **UI Testing** 标签（合并布局）中，找到 Analyzer 配置区域的 **对称采集** 复选框
2. 勾选 **对称采集**
3. 配置 **Original** 的 Header(s) to Replace：填写用户 A 的 Cookie
4. 配置 **Session1** 的 Headers to Replace：填写用户 B 的 Cookie
5. 配置 **Target URL**（目标页面）
6. 点击 **▶ Start** 启动 Analyzer

#### 步骤 2：一键执行 Run1 + Run2

1. 点击 **抓取并点击页面链接** 按钮
2. 系统自动执行：
   - **Run1**：使用 Original（用户 A）的 Cookie 访问 Target URL，点击页面链接，采集 A 的流量
   - **Run2**：Run1 完成后自动交换配置，使用 Session1（用户 B）的 Cookie 再次抓取相同页面，采集 B 的流量
3. 无需手动点击「对称采集 Run2」，全程自动化

#### 步骤 3：查看 Bypass 状态

1. 抓取完成后，切换到 **Result** 标签
2. 点击 **刷新** 按钮
3. 表格会多出一列 **Bypass**，显示每个 endpoint 的越权类型：
   - **Trivial**：Resp_A == Resp_B，平凡响应，与权限无关
   - **Vertical**：疑似垂直越权（仅一方有 Original 流量，另一方通过重放拿到数据）
   - **Horizontal**：疑似水平越权（双方都有 Original 且 Resp_A ≠ Resp_B，任一侧重放为 SAME/SIMILAR）
   - **Unknown**：无法判定

3. 勾选 **排除 Trivial** 可默认过滤掉平凡响应，只关注 Vertical/Horizontal
4. 点击 **刷新** 按钮重新计算 Bypass 状态

### 身份识别说明

启用对称采集后，系统仅分析**匹配当前 Original 的 headers** 的请求：
- Run1：只分析包含用户 A 的 Cookie 的请求
- Run2：只分析包含用户 B 的 Cookie 的请求

不匹配的请求不会被送入 Analyzer，避免 A、B 流量混在一起。

### 清空对称数据

- 点击 **清空对称数据** 按钮可清空 SymmetricTrafficStore，退出 Run2 模式
- 清空后需重新执行抓取才能再次使用 Bypass 状态

### 手动 Run2（可选）

- 若需手动触发 Run2（如 Run1 非通过抓取完成），可点击 **对称采集 Run2** 按钮
- 正常流程下无需点击，抓取会自动完成 Run1 → Run2

### 注意事项

1. 需先配置 **Target URL**，抓取才能正确执行
2. Run1 与 Run2 会访问**相同**的 Target URL 和链接，确保 endpoint 一致
3. Original 与 Session1 分别填写用户 A、B 的 Cookie

---

## 💡 常见使用场景

### 场景 1：检测 CSRF Token 绕过

**目标**：检测应用是否正确验证 CSRF Token

**步骤**：
1. 创建两个 Session：
   - `Session1`：使用有效的 CSRF Token
   - `Session2`：删除 CSRF Token 或使用无效 Token
2. 配置 Token：
   - 添加 `csrf_token` 参数
   - 设置为 `Auto Extract`，从响应 Body 中提取
3. 启动 Analyzer，发送包含 CSRF Token 的请求
4. 查看结果：
   - 如果 `Session2` 的状态是 `SAME`，说明存在 CSRF Token 绕过

### 场景 2：检测 Session 固定漏洞

**目标**：检测应用是否允许使用其他用户的 Session

**步骤**：
1. 创建两个 Session：
   - `Session1`：使用用户 A 的 Session Cookie
   - `Session2`：使用用户 B 的 Session Cookie
2. 配置 Session Cookie：
   - 在 `Headers to Replace` 中添加：
     ```
     Cookie: session=userA_session_id
     ```
   - 在 `Session2` 中替换为：
     ```
     Cookie: session=userB_session_id
     ```
3. 启动 Analyzer，发送需要认证的请求
4. 查看结果：
   - 如果两个 Session 都能访问相同内容，说明存在 Session 固定漏洞

### 场景 3：检测认证头绕过

**目标**：检测删除认证头后是否仍能访问资源

**步骤**：
1. 创建一个 Session：
   - 勾选 `Remove Headers`
   - 添加要删除的 Header：`Authorization`
2. 启动 Analyzer，发送需要认证的请求
3. 查看结果：
   - 如果删除认证头后仍返回 `SAME`，说明存在认证绕过

### 场景 4：自动化 UI 测试（越权检测）

**目标**：使用不同身份自动测试 Web 应用功能，检测越权漏洞

**步骤**：

**1. 在 Analyzer 中配置多个 Session（不同用户身份）**

- 点击 `+` 创建 Session，例如：`普通用户`、`管理员`
- 在每个 Session 的 **Headers to Replace** 中填写该身份的 Cookie，格式为完整请求头，每行一个，例如：

  **Session「普通用户」**：
  ```
  Cookie: tiup_uid=66ab983090f4d1021d906b86; session=133e71c75b2d4db584588fd2992053e9
  ```

  **Session「管理员」**：
  ```
  Cookie: tiup_uid=管理员uid值; session=管理员session值
  ```

- 说明：Analyzer 会用每个 Session 的 Headers to Replace **替换**原始请求中的对应请求头，再重发请求。因此每个 Session 应填写该身份的真实 Cookie 值（从浏览器开发者工具或抓包中获取）。

- **Original 与 Session 的 Headers to Replace**：结构相同（header 名一致），值不同。Original 填用户 A 的认证信息；各 Session 填用户 B、C 等的认证信息。差分检测需不同身份才能发现越权。

**2. 启动 Analyzer**

- 点击 `▶ Start` 按钮

**3. 在 UI Testing 中**：
   - 配置 Target URL
   - 在 Original 的 Header(s) to Replace 中填写任意一个身份的认证信息（用于访问目标页面）
   - 点击「抓取并点击页面链接」（代理会自动启动）

**4. （可选）切换用户再次抓取**：
   - 若不同角色看到的链接不同（如管理员可见更多菜单），可修改 Original 的 Headers to Replace 为另一用户，再次点击抓取，以发现更多 URL
   - 若页面链接对所有角色相同，则无需此步骤

**5. 查看结果**：
   - 在 Analyzer 表格中查看各 Session 的 SAME/SIMILAR/DIFFERENT 状态（比较对象是：同一个原始请求 的「原始响应」与「各 Session 重发后的响应」两两比较。）
   - 若不同身份的 Session 出现 SAME 或 SIMILAR，可能存在越权

**6. （进阶）使用对称采集筛掉平凡响应**：
   - 若结果中混杂大量对任意身份都相同的 API，可启用 **对称采集**
   - 勾选「对称采集」→ Run1 用用户 A 访问 → 点击「对称采集 Run2」→ Run2 用用户 B 访问相同 API
   - 在 Result 标签查看 Bypass 列（Trivial/Vertical/Horizontal），勾选「排除 Trivial」可过滤平凡结果

---

## ⚙️ 高级配置

### 全局设置

在 Burp Suite 菜单栏 → `AuthAnalyzer` → `Settings` 中可以配置：

1. **线程数（Number of Threads）**
   - 控制并发分析的请求数量
   - 默认值：根据系统性能自动设置

2. **请求间延时（Delay Between Requests）**
   - 每个请求之间的延时（毫秒）
   - 用于避免对服务器造成过大压力

3. **响应比较规则**
   - **Respect Response Code for Same Status**：
     - 勾选：响应体相同但状态码不同时，不判定为 `SAME`
     - 不勾选：只要响应体相同就判定为 `SAME`
   
   - **Respect Response Code for Similar Status**：
     - 勾选：响应长度相似但状态码不同时，不判定为 `SIMILAR`
   
   - **Deviation for Similar Status**：
     - 相似响应的长度偏差百分比（默认 5%）

4. **Prompt for Input 时的线程数**
   - 如果 Session 中有 Token 需要手动输入，自动将线程数设为 1
   - 避免多个对话框同时弹出

### 数据持久化

- Analyzer 会自动保存配置：
  - Session 配置
  - Token 配置
  - 过滤器配置
  - 对称采集开关（symmetricCaptureEnabled）
- 扩展卸载时会自动保存
- 重新加载扩展时会自动恢复

### 右键菜单功能

在 Burp Suite 的其他工具（Proxy、Repeater 等）中，右键点击请求，可以看到 `AuthAnalyzer` 菜单：

1. **Repeat Requests with Filter Options**
   - 使用过滤器选项重复请求

2. **Set Parameters Automatically**
   - 自动从选中的请求中提取 Token/参数
   - 可以添加到现有 Session 或创建新 Session

3. **Add Header to Session**
   - 将请求头添加到指定 Session

---

## 🐛 常见问题

### Q1: Analyzer 没有拦截请求？

**可能原因**：
- Analyzer 未启动（检查 `Start` 按钮状态）
- 请求被过滤器过滤掉了
- 请求不在 Scope 中（如果配置了 Scope 限制）

**解决方法**：
- 检查 Analyzer 状态是否为 `Running`
- 检查过滤器配置
- 检查 Scope 设置

### Q2: Token 提取失败？

**可能原因**：
- Token 位置配置错误
- 字段名不匹配
- 响应格式变化

**解决方法**：
- 检查 Token Location 设置
- 检查提取字段名是否正确
- 使用 `From To String` 方式手动提取
- 查看响应内容，确认 Token 格式

### Q3: UI Testing 表格没有内容？

**可能原因**：

1. **Analyzer 未启动**：必须先在 Analyzer 标签点击 Start
2. **未配置 Target URL**：抓取前需填写目标 URL
3. **目标不在 Burp Scope**：若勾选了「Only In Scope」过滤器，目标域名（如 `v.ruc.edu.cn`）必须已加入 Burp 的 Target → Scope，否则所有请求会被过滤
4. **抓取点击失败**：日志出现 `stale element reference` 时，说明大量链接点击失败，产生的 HTTP 请求很少，表格自然为空

**解决方法**：
- 先在 Analyzer 标签点击 `▶ Start` 启动 Analyzer
- 配置 Target URL 后点击「抓取并点击页面链接」（代理会自动启动）
- 在 Burp 的 **Target** → **Scope** 中，将目标域名加入 **Include in scope**；或临时取消勾选「Only In Scope」
- 若日志有大量 `stale element reference`，多为 SPA/动态页面导致，可尝试选择链接较少的页面抓取

### Q4: UI Testing 出现 "invalid session id"？

**可能原因**：浏览器窗口被手动关闭，但程序仍持有旧会话引用

**解决方法**：
- 再次点击「抓取并点击页面链接」，程序会自动检测并重启浏览器
- 若仍失败，重启 Burp Suite 后重试

### Q5: UI Testing 无法启动浏览器？

**可能原因**：
- ChromeDriver 未正确下载
- 浏览器路径配置错误
- 端口冲突

**解决方法**：
- 检查网络连接（需要下载 ChromeDriver）
- 检查 Burp Proxy 端口（默认 8080）
- 查看错误日志

### Q6: 响应比较不准确？

**可能原因**：
- 响应包含动态内容（时间戳、随机数等）
- 响应长度偏差设置不合理

**解决方法**：
- 调整 `Deviation for Similar Status` 参数
- 使用 `Match and Replace` 功能过滤动态内容
- 手动检查响应内容

### Q7: 对称采集 Run2 按钮不可用？

**可能原因**：
- Analyzer 未启动
- 未勾选「对称采集」
- 已处于 Run2 模式（需先点击「清空对称数据」）
- 未创建任何 Session

**解决方法**：
- 确保 Analyzer 已点击 Start 且处于运行状态
- 勾选「对称采集」复选框
- 至少创建一个 Session（如 user1）

### Q8: 对称采集后 Bypass 列全是 Unknown？

**可能原因**：
- Run1 与 Run2 访问的 API 不完全一致（endpoint 不同）
- 仅完成 Run1 未完成 Run2，或反之
- 身份识别未匹配（请求 headers 与 Original 配置不一致）

**解决方法**：
- 确保两个浏览器访问**相同**的 URL 和 API
- 完整执行 Run1 → 点击 Run2 → Run2 的流程
- 检查 Original 的 Headers to Replace 是否与当前浏览器的 Cookie 一致

### Q9: 内存占用过高？

**可能原因**：
- 分析了大量请求
- 响应内容过大

**解决方法**：
- 定期清理表格（`Clear Table`）
- 使用过滤器减少分析的请求数量
- 重启 Burp Suite

---

## 📖 最佳实践

1. **逐步测试**
   - 先创建简单的 Session 测试基本功能
   - 逐步添加复杂的 Token 配置

2. **使用过滤器**
   - 只分析相关的请求，提高效率
   - 减少不必要的分析

3. **合理设置线程数**
   - 不要设置过高的线程数，避免对目标服务器造成压力
   - 根据网络延迟调整请求间延时

4. **定期清理数据**
   - 测试完成后清理表格，释放内存
   - 导出重要数据后删除

5. **保存配置**
   - 重要的 Session 配置要及时保存
   - 使用 `Export` 功能备份配置

6. **结合其他工具**
   - 与 Burp Suite 的 Scanner、Intruder 等工具配合使用
   - 使用 Repeater 手动验证可疑结果

7. **越权检测时使用对称采集**
   - 当 SAME/SIMILAR 结果较多且混杂平凡响应时，启用对称采集
   - 确保 Run1 与 Run2 访问相同的 API，以便正确比较 Resp_A 与 Resp_B
   - 在 Result 标签勾选「排除 Trivial」聚焦于 Vertical/Horizontal 越权

---

## 🔗 相关资源

- **项目架构文档**：`项目架构与开发指南.md`
- **对称采集设计文档**：`docs/SessionTrafficCapture-Design.md`
- **Burp Suite 官方文档**：https://portswigger.net/burp/documentation
- **Selenium 文档**：https://www.selenium.dev/documentation/

---

**文档版本**：1.2  
**最后更新**：2026-02-25
