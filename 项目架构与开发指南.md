# AuthAnalyzer 项目架构与开发指南

## 📋 项目总体介绍

这是一个 **Burp Suite 扩展项目：AuthAnalyzer**，基于 Java 8 + Maven 构建。项目主要功能分为两个核心模块：

### 核心功能模块

1. **Analyzer（认证分析器）**
   - 拦截/重复 HTTP 请求
   - 对不同"会话(Session)"下的请求头、Token 等进行修改
   - 比较修改后的响应与原始响应
   - 判断是否存在认证绕过（Bypass）漏洞

2. **UI Testing（UI 自动化测试）**
   - 在 Burp 中集成基于 Selenium WebDriver 的 UI 自动化测试面板
   - 可以复用 Analyzer 采集到的请求数据
   - 支持自动化浏览器操作和请求重放

### 项目配置（pom.xml）

**构建配置**：
- `sourceDirectory` = `src`（传统布局，非标准 Maven 结构）
- 使用 `maven-compiler-plugin` 编译 Java 1.8
- 使用 `maven-assembly-plugin` 生成 `jar-with-dependencies`
- 打包后的 Jar 文件位于 `target` 目录

**核心依赖**：
- `burp-extender-api` (2.3) - Burp Suite 扩展 API
- `gson` (2.8.6) - JSON 序列化/反序列化
- `jsoup` (1.13.1) - HTML 解析（用于 Token 提取）
- `selenium-java` (4.35.0) - WebDriver 自动化
- `webdrivermanager` (6.3.2) - 自动管理浏览器驱动

**使用方式**：
将 `target/AuthAnalyzer-1.1.14-jar-with-dependencies.jar` 加载到 Burp Suite 的 Extender 中即可。

---

## 🏗️ 代码结构详解

### 1. `burp` 包 - 扩展入口

#### `BurpExtender.java`
- **职责**：扩展的主入口类，实现 `IBurpExtender`, `ITab`, `IExtensionStateListener`
- **关键功能**：
  - 创建主界面 `MainPanel`
  - 创建 Burp 内部的 `JTabbedPane`，包含两个标签：
    - `"Analyzer"` → 原有的 AuthAnalyzer 界面
    - `"UI Testing"` → 新的 UI 自动化界面 `UITestingPanel`
  - 注册 HTTP/Proxy 监听器 `HttpListener`
  - 注册扩展卸载回调，用于保存配置
  - 在 Burp 顶部菜单栏添加 `AuthAnalyzerMenu`

### 2. `com.protect7.authanalyzer.controller` 包 - 控制器层

#### `HttpListener.java`
- **职责**：HTTP 请求/响应拦截器
- **实现接口**：`IHttpListener`, `IProxyListener`
- **工作流程**：
  1. 检查全局配置 `CurrentConfig.isRunning()` 和 `isDropOriginal()`
  2. 通过 `RequestFilter` 列表过滤请求（方法/路径/状态码/范围等）
  3. 将符合条件的请求提交到分析线程池：`CurrentConfig.performAuthAnalyzerRequest(messageInfo)`

#### `RequestController.java`
- **职责**：请求分析的核心控制器
- **核心方法**：`analyze(IHttpRequestResponse originalRequestResponse)`
- **处理流程**：
  1. **准备阶段**：
     - 获取新的 `mapId`（作为原始请求的唯一标识）
     - 解析原始请求和响应信息
   
  2. **遍历所有 Session**：
     - 检查 Session 状态（是否运行中、是否过滤等）
     - 使用 `RequestModifHelper` 根据 Session 配置修改请求
     - 发送修改后的请求：`callbacks.makeHttpRequest(...)`
     - 使用 `ExtractionHelper` 从响应中提取 Token
     - 调用 `analyzeResponse()` 比较原始响应与 Session 响应
     - 生成绕过状态：`SAME` / `SIMILAR` / `DIFFERENT` / `NA`
     - 将结果存储到 `Session.requestResponseMap`
   
  3. **记录原始请求**：
     - 创建 `OriginalRequestResponse` 对象
     - 添加到 `CurrentConfig.tableModel`（`RequestTableModel`）
     - 触发 UI 更新

- **绕过判定逻辑**（`analyzeResponse` 方法）：
  - `SAME`：响应体完全相同（且状态码规则满足）
  - `SIMILAR`：状态码相同 & 响应长度在 ±deviation% 范围内
  - `DIFFERENT`：其他情况

### 3. `com.protect7.authanalyzer.util` 包 - 工具类

#### `CurrentConfig.java`（全局单例）
- **职责**：全局配置和状态管理
- **核心数据**：
  - `sessions`：所有会话列表
  - `requestFilterList`：请求过滤器列表
  - `tableModel`：中心表格的数据模型
  - `running`：运行状态开关
  - `dropOriginal`：是否丢弃原始请求
  - 响应比较规则配置
  - 线程池 `analyzerThreadExecutor`
  - 请求间延时配置

- **核心方法**：
  - `performAuthAnalyzerRequest(messageInfo)`：将请求提交到线程池异步处理
  - `setRunning(boolean)`：启动/停止分析器
  - `addSession(Session)` / `addRequestFilter(RequestFilter)`：添加会话和过滤器

#### 其他工具类
- `RequestModifHelper`：根据 Session 配置修改请求
- `ExtractionHelper`：从响应中提取 Token
- `DataStorageProvider`：配置持久化
- `Setting`：设置项管理
- `Globals`：全局常量

### 4. `com.protect7.authanalyzer.entities` 包 - 实体类

#### `Session.java`
- **职责**：会话级别的配置和状态
- **核心属性**：
  - `headersToReplace` / `headersToRemove`：请求头替换/删除配置
  - `filterRequestsWithSameHeader`：是否过滤相同 Header 的请求
  - `restrictToScope` / `scopeUrl`：范围限制
  - `testCors`：是否测试 CORS
  - `tokens`：Token 列表
  - `requestResponseMap`：`mapId -> AnalyzerRequestResponse` 映射
  - `statusPanel`：GUI 状态面板引用

#### `AnalyzerRequestResponse.java`
- **职责**：单个 Session 下一次"重复请求"的结果
- **属性**：
  - `requestResponse`：修改后的请求/响应
  - `status`：绕过状态（`BypassConstants`）
  - `infoText`：错误信息（如果请求失败）
  - `statusCode` / `responseContentLength`：响应状态码和内容长度

#### `OriginalRequestResponse.java`
- **职责**：原始请求（用户真实发出的）及其基本属性
- **属性**：
  - `id`：mapId
  - `method` / `host` / `url`：请求基本信息
  - `statusCode` / `responseContentLength`：响应信息
  - `comment` / `marked`：用户标记和备注

#### 其他实体类
- `Token`：Token 配置（位置、提取规则等）
- `MatchAndReplace`：匹配替换规则
- `TokenLocation`：Token 位置枚举
- `Range`：范围定义

### 5. `com.protect7.authanalyzer.filter` 包 - 过滤器

各种请求过滤器实现：
- `MethodFilter`：HTTP 方法过滤
- `PathFilter`：路径过滤
- `QueryFilter`：查询参数过滤
- `StatusCodeFilter`：状态码过滤
- `FileTypeFilter`：文件类型过滤
- `InScopeFilter`：范围过滤
- `OnlyProxyFilter`：仅代理请求过滤

**统一接口**：`RequestFilter.filterRequest(...)`

### 6. `com.protect7.authanalyzer.gui.main` 包 - 主界面

#### `MainPanel.java`
- **布局**：
  - 上半部分：`ConfigurationPanel`（配置 Session、Token、过滤器等）
  - 下半部分：`CenterPanel`（结果表格 + 请求/响应视图）
- **初始化**：
  - 注册右键菜单工厂 `ContextMenuController`
  - 加载自动存储的配置

#### `CenterPanel.java`
- **左侧**：`JTable` + `RequestTableModel`
  - 显示所有 `OriginalRequestResponse`
  - 支持过滤（按状态、重复、标记等）
  - 支持搜索（路径/请求/响应）
  - 支持数据导出
  - 右键菜单：标记/取消标记、重复请求、删除行、添加注释

- **右侧**：`RequestResponsePanel`（两个）+ Diff 区域
  - 每个 `RequestResponsePanel` 包含多个 `SessionTabbedPane`
  - 每个 Session 有 `Request` / `Response` 子标签
  - 支持 Compare View（双面板对比）+ Diff View（差异高亮）

#### `RequestResponsePanel.java`
- **职责**：封装 Session Tabs
- **功能**：同步两个 Panel 的 Tab 切换，用于 Diff 对比

### 7. `com.protect7.authanalyzer.gui.UITesting` 包 - UI 测试界面

#### `UITestingPanel.java`
- **布局**：
  - 左侧 `ControlsPanel`：控制 UI 自动化参数和操作
  - 中间 `RequestTablePanel`：绑定 `RequestTableModel`，复用 Analyzer 数据
  - 右侧 `DetailPanel`：显示选中行的详细信息

- **数据绑定**：
  - 定时（500ms）从 `CurrentConfig.getTableModel()` 获取模型
  - 动态绑定到 `RequestTablePanel`
  - 同步更新 Session 列表

### 8. `com.protect7.authanalyzer.uitesting.runner` 包 - UI 测试执行器

#### `ProxyDriverManager.java`
- **职责**：管理带代理的 WebDriver 实例
- **功能**：启动/停止 WebDriver，配置代理

#### `UITestRunner.java`
- **职责**：执行 UI 自动化测试
- **工作流程**：
  1. 使用 `WebDriverManager` 自动下载和管理 ChromeDriver
  2. 启动 Chrome 浏览器
  3. 访问 `baseUrl`，注入 Cookie（`tiup_uid` 和 `session`）
  4. 访问 `targetUrl`
  5. 查找所有 `href='javascript:void(0)'` 的链接
  6. 逐个点击链接，然后返回 `targetUrl`
  7. 实现"带身份"的自动遍历

---

## 🔄 工作流与数据流

### 1. Burp 加载扩展

```
Burp Suite
  └─> BurpExtender.registerExtenderCallbacks()
      ├─> 创建 MainPanel
      ├─> 创建 burpTabbedPane（Analyzer + UI Testing）
      ├─> 注册 HttpListener（HTTP + Proxy）
      └─> 注册 ExtensionStateListener
```

### 2. 请求进入流程

```
HTTP 请求经过 Burp
  └─> HttpListener.processHttpMessage()
      ├─> 检查 CurrentConfig.isRunning()
      ├─> 检查 RequestFilter 列表
      └─> CurrentConfig.performAuthAnalyzerRequest()
          └─> 提交到线程池
              └─> RequestController.analyze()
```

### 3. RequestController 分析流程

```
RequestController.analyze(originalRequestResponse)
  ├─> 获取 mapId，解析原始请求/响应
  ├─> 遍历所有 Session
  │   ├─> 检查 Session 状态（运行中/过滤规则）
  │   ├─> RequestModifHelper.getModifiedRequest()
  │   ├─> callbacks.makeHttpRequest() 发送修改后的请求
  │   ├─> ExtractionHelper 提取 Token
  │   ├─> analyzeResponse() 比较响应
  │   └─> 存储到 Session.requestResponseMap
  └─> 创建 OriginalRequestResponse，添加到 tableModel
```

### 4. UI 更新流程

```
tableModel.addNewRequestResponse()
  └─> CenterPanel 表格刷新
      └─> 用户选择行
          └─> changeRequestResponseView()
              ├─> 显示原始请求/响应
              └─> 显示各 Session 的修改后请求/响应
                  └─> Diff View（可选）
```

### 5. UI Testing 数据绑定

```
UITestingPanel（定时 500ms）
  └─> CurrentConfig.getTableModel()
      ├─> 绑定到 RequestTablePanel
      ├─> 刷新 Session 列表
      └─> refreshSelectedRowDetails()
          ├─> DetailPanel.showOriginal()
          └─> DetailPanel.showSession()
```

---

## 🔌 暴露的 API

### 对 Burp 暴露的接口

- `BurpExtender`：
  - `registerExtenderCallbacks(IBurpExtenderCallbacks)`
  - `getTabCaption()` / `getUiComponent()`
  - `extensionUnloaded()`

- `HttpListener`：
  - `processHttpMessage(int toolFlag, boolean messageIsRequest, IHttpRequestResponse)`
  - `processProxyMessage(boolean messageIsRequest, IInterceptedProxyMessage)`

### 内部可复用的 Java API

- `CurrentConfig`：
  - `getCurrentConfig()`：获取单例
  - `setRunning(boolean)`：启动/停止分析器
  - `addSession(Session)` / `addRequestFilter(RequestFilter)`：添加配置
  - `performAuthAnalyzerRequest(IHttpRequestResponse)`：手动触发分析

- `RequestController`：
  - `analyze(IHttpRequestResponse)`：直接分析一个 HTTP 消息

- UI Testing：
  - `UITestRunner.run(baseUrl, targetUrl, tiupUid, session, stdout, stderr)`：执行 UI 自动化
  - `ProxyDriverManager.startDriver(...)` / `stopDriver()`：管理 WebDriver

---

## 🚀 扩展开发指南

### 扩展方向 1：增强 Bypass 判定逻辑

**适用场景**：需要更精细的绕过判定（如关键字段比较、业务状态码判断等）

**核心位置**：`RequestController.analyzeResponse(...)`

**实现步骤**：
1. 在 `BypassConstants` 中新增状态枚举（如 `PARTIAL`, `ERROR_PAGE`, `LOGIN_PAGE`）
2. 在 `analyzeResponse` 中添加新的判定分支
3. 更新 UI 显示和过滤逻辑（`CenterPanel`, `RequestTableModel`, `BypassCellRenderer`）
4. 更新数据导出功能（`DataExportDialog`）

**示例**：
```java
// 在 analyzeResponse 中添加 JSON 字段比较
if (originalResponseInfo.getStatedMimeType().equals("application/json")) {
    // 解析 JSON，比较关键字段
    // 返回新的 BypassConstants
}
```

### 扩展方向 2：新增请求过滤条件

**适用场景**：只分析特定类型的请求（如特定 MIME 类型、自定义 Header、特定请求体格式）

**核心位置**：
- `com.protect7.authanalyzer.filter.*`
- `HttpListener.isFiltered(...)`

**实现步骤**：
1. 创建新的 Filter 类，实现 `RequestFilter` 接口
2. 在 `filterRequest` 中实现过滤逻辑
3. 在配置初始化处（如 `ConfigurationPanel`）添加到 `CurrentConfig.requestFilterList`
4. 可选：在 UI 中添加配置控件

**示例**：
```java
public class CustomHeaderFilter implements RequestFilter {
    @Override
    public boolean filterRequest(IBurpExtenderCallbacks callbacks, int toolFlag, 
                                 IRequestInfo requestInfo, IResponseInfo responseInfo) {
        List<String> headers = requestInfo.getHeaders();
        // 检查特定 Header
        return headers.stream().anyMatch(h -> h.startsWith("X-Custom-"));
    }
}
```

### 扩展方向 3：扩展 Session/Token 能力

**适用场景**：支持更多类型的 Token、更复杂的提取/替换策略

**核心类**：
- `Token`, `TokenLocation`, `ExtractionHelper`, `RequestModifHelper`
- `TokenSettingsDialog`, `TokenPanel`

**实现步骤**：
1. 在 `TokenLocation` 枚举中新增位置类型（如 `JSON_BODY_FIELD`）
2. 在 `ExtractionHelper` 中实现新的提取函数
3. 在 `RequestModifHelper` 中实现新的写入逻辑
4. 更新 GUI（`TokenSettingsDialog`）支持新类型
5. 确保序列化兼容（`DataStorageProvider` / Gson）

### 扩展方向 4：新增 UI 展示/操作功能

**适用场景**：统计视图、图表、批量操作等

**核心位置**：`CenterPanel`, `RequestResponsePanel`, `UITestingPanel`

**实现示例**：
- 在 `CenterPanel` 添加统计面板（总请求数、各状态占比、按 Host/Path 分布）
- 在表格右键菜单添加新操作（批量复制为 curl、导出为 Postman 集合）
- 添加图表展示（使用 JFreeChart 或其他图表库）

### 扩展方向 5：扩展 UI Testing 模块

**适用场景**：
- 根据 Analyzer 检测结果自动生成点击路径
- 对特定 Session 的请求进行浏览器重放
- 支持多浏览器、Headless 模式、自定义 Proxy

**核心类**：
- `UITestingPanel`, `ProxyDriverManager`, `UITestRunner`

**实现步骤**：
1. 扩展 `ProxyDriverManager`：
   - 支持 Firefox/Edge 等多种 Driver
   - 支持自定义 Proxy 或 No-Proxy 模式
2. 创建新的 TestRunner：
   - 实现更复杂的测试流程
   - 在 `ControlsPanel` 中添加模式选择
3. 与 Analyzer 数据联动：
   - 添加"在浏览器中重放选中请求"功能
   - 利用 Burp Repeater + 代理转发实现

---

## 📝 开发步骤建议

### 第一次接触项目

1. **环境准备**：
   ```bash
   mvn package
   ```
   将生成的 `target/AuthAnalyzer-1.1.14-jar-with-dependencies.jar` 加载到 Burp Suite

2. **熟悉现有功能**：
   - 打开 Analyzer 标签，了解配置界面
   - 创建测试 Session，观察请求分析流程
   - 打开 UI Testing 标签，了解自动化测试功能

3. **阅读关键代码**：
   - `BurpExtender.java`：入口
   - `HttpListener.java`：请求拦截
   - `RequestController.java`：核心分析逻辑
   - `CurrentConfig.java`：全局配置

### 小改动练习

**示例 1**：在 `analyzeResponse` 中添加新状态
- 响应长度为 0 时返回特殊状态
- 在 UI 中显示该状态

**示例 2**：添加简单的 RequestFilter
- 只允许 `toolFlag == TOOL_PROXY` 的请求进入分析

### 中等改动

- 为 Token 提取添加新规则
- 在 UI Testing 中添加新的 Runner（如点击所有 `<button>` 标签）

### 大改动/重构

- UI Testing 与 Analyzer 智能联动（根据 Bypass 结果自动引导测试）
- 将逻辑拆分为可单测的独立模块（增加 JUnit 测试）

---

## 📚 相关资源

- **Burp Suite Extender API 文档**：https://portswigger.net/burp/extender/api
- **Selenium WebDriver 文档**：https://www.selenium.dev/documentation/
- **项目版本**：1.1.14

---

## ⚠️ 注意事项

1. **线程安全**：`CurrentConfig` 是单例，多线程访问时注意同步
2. **内存管理**：大量请求可能导致内存占用过高，注意清理 `requestResponseMap`
3. **性能优化**：线程池大小和请求间延时需要根据实际情况调整
4. **配置持久化**：扩展卸载时会自动保存配置，确保序列化正确

---

## 🤝 贡献指南

如需扩展功能，建议：
1. 先理解现有架构和代码风格
2. 从小改动开始，逐步熟悉项目
3. 保持代码风格一致
4. 添加必要的注释和文档
5. 测试新功能是否影响现有功能

---

**文档版本**：1.0  
**最后更新**：2026-02-18
